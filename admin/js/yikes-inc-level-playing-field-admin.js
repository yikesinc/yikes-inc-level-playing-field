(function( $ ) {
	'use strict';


})( jQuery );

/**
 * Update the applicant status based on the button clicked
 * @since 1.0.0
 */
function toggleApplicantStatus( clicked_button, applicant_id ) {
	alert( 'This is still in progress, but will toggle the status for the clicked applicant. Clicked User: ' + applicant_id );
}
